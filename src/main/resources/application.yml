server:
  port: 8082

spring:
  main:
    web-application-type: servlet
  datasource:
    data: data.sql
    url: jdbc:mariadb://localhost:3306/registries
    username: root
    password: admin
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB106Dialect
  sql:
    init:
      mode: always
